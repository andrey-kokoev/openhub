# OpenHub Remote Mode Configuration
# These variables are required when running in remote mode (nuxt dev --remote)

# URL of your deployed Cloudflare Worker
OPENHUB_REMOTE_URL=https://example-cf-nitro-nuxt.your-subdomain.workers.dev

# Shared secret for authenticating proxy requests
# This same secret must be set in your deployed worker's environment
# Generate a secure secret: openssl rand -base64 32
OPENHUB_REMOTE_SECRET=your-shared-secret-here